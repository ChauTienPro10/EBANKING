version: '3.8'

services:
  redis:
    image: redis:7.2
    container_name: redis-ebanking
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data
    networks:
      - ebanking-network

volumes:
  redis-data:

networks:
  ebanking-network:
    external: true
